namespace Definitions
{
    public enum Layer
    {
        Undefined,
        Ground,
        Count
    }

    public enum GroundTile
    {
        Undefined,
        Grassland,
        Water,
        Count
    }
}
